# src/assets

App-specific assets consumed by feature UI (fonts, in-app images) — distinct
from the top-level `frontend/assets/` folder, which holds the app icon/splash
files Expo's `app.json` references directly and expects at that fixed path.
