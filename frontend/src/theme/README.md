# theme

Shared design tokens (colors, spacing, typography) once real UI work begins.
Deliberately left undesigned in this scaffolding pass — this task's scope is
architecture, not visual design (see the task's "Do NOT build actual UI
designs" constraint).
