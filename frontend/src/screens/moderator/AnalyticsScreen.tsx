import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET /api/analytics* — non-financial fields only for MODERATOR. */
export default function AnalyticsScreen() {
  return <PlaceholderScreen title="Analytics" description="GET /api/analytics/dashboard" />;
}
