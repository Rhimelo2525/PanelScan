import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** View/update assigned projects only — schedule/notes fields (403 on other fields). */
export default function ProjectsScreen() {
  return <PlaceholderScreen title="Projects" description="GET /api/projects" />;
}
