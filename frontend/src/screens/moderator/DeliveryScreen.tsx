import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** Full CRUD: POST /api/delivery, PATCH /:id, PATCH /:id/delivered, DELETE /:id. */
export default function DeliveryScreen() {
  return <PlaceholderScreen title="Delivery" description="POST /api/delivery" />;
}
