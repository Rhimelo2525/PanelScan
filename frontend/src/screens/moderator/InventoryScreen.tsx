import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET/PATCH /api/inventory* — add/reduce/reserve/release, low-stock report. */
export default function InventoryScreen() {
  return <PlaceholderScreen title="Inventory" description="GET /api/inventory" />;
}
