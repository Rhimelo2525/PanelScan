import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** Full CRUD on /api/installers — MODERATOR-only (OWNER has zero access here). */
export default function InstallersScreen() {
  return <PlaceholderScreen title="Installers" description="GET /api/installers" />;
}
