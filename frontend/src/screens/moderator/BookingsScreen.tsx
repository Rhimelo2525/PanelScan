import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET /api/bookings/all, approve/assign-installer/status updates. */
export default function BookingsScreen() {
  return <PlaceholderScreen title="Bookings" description="GET /api/bookings/all" />;
}
