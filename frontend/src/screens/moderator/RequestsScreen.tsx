import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** POST /api/requests, view/edit/cancel/delete own — cannot approve/reject (OWNER-only). */
export default function RequestsScreen() {
  return <PlaceholderScreen title="Requests" description="POST /api/requests" />;
}
