import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET /api/analytics/dashboard — operational subset, no financial fields for MODERATOR. */
export default function DashboardScreen() {
  return <PlaceholderScreen title="Dashboard" description="GET /api/analytics/dashboard" />;
}
