import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** POST /api/auth/login — see FRONTEND_HANDOFF.md §3.1. */
export default function LoginScreen() {
  return <PlaceholderScreen title="Login" description="POST /api/auth/login" />;
}
