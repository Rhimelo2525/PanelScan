import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET /api/feedback* — view-only oversight, same access as MODERATOR. */
export default function FeedbackScreen() {
  return <PlaceholderScreen title="Feedback" description="GET /api/feedback (view-only)" />;
}
