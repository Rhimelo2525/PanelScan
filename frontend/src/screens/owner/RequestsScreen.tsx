import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET /api/requests (all), PATCH /:id/approve or /reject, DELETE any request. */
export default function RequestsScreen() {
  return <PlaceholderScreen title="Requests" description="GET /api/requests" />;
}
