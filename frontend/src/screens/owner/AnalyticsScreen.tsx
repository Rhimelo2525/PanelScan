import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET /api/analytics/sales, /products, /customers, /projects — full fields for OWNER. */
export default function AnalyticsScreen() {
  return <PlaceholderScreen title="Analytics" description="GET /api/analytics/sales" />;
}
