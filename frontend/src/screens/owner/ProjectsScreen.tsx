import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** Full CRUD: create, assign/reassign, status, delete — OWNER-only for all of these. */
export default function ProjectsScreen() {
  return <PlaceholderScreen title="Projects" description="POST /api/projects" />;
}
