import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** Same access as MODERATOR. */
export default function OrdersScreen() {
  return <PlaceholderScreen title="Orders" description="GET /api/orders" />;
}
