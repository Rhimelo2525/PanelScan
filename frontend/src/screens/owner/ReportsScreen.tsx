import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET /api/reports* — full fields including financial figures for OWNER. */
export default function ReportsScreen() {
  return <PlaceholderScreen title="Reports" description="GET /api/reports" />;
}
