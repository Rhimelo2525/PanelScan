import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET/POST/PATCH /api/bookings* — CUSTOMER's own bookings. */
export default function BookingsScreen() {
  return <PlaceholderScreen title="Bookings" description="GET /api/bookings" />;
}
