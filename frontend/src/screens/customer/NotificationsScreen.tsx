import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET /api/notifications* — always scoped to the caller's own notifications. */
export default function NotificationsScreen() {
  return <PlaceholderScreen title="Notifications" description="GET /api/notifications" />;
}
