import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** POST/GET/PATCH/DELETE /api/feedback* — own, DELIVERED orders only. */
export default function FeedbackScreen() {
  return <PlaceholderScreen title="Feedback" description="POST /api/feedback" />;
}
