import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET/POST /api/chat* — CUSTOMER participates in their own conversations. */
export default function ChatScreen() {
  return <PlaceholderScreen title="Chat" description="GET /api/chat" />;
}
