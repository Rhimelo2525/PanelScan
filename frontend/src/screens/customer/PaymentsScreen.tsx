import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** POST /api/payments/create — redirects to a PayMongo-hosted checkoutUrl. */
export default function PaymentsScreen() {
  return <PlaceholderScreen title="Payments" description="POST /api/payments/create" />;
}
