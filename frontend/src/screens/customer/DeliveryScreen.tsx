import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET /api/delivery — tracking view for the customer's own orders. */
export default function DeliveryScreen() {
  return <PlaceholderScreen title="Delivery" description="GET /api/delivery" />;
}
