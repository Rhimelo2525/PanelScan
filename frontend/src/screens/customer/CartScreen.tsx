import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

/** GET/POST/PATCH/DELETE /api/cart* — CUSTOMER-only. */
export default function CartScreen() {
  return <PlaceholderScreen title="Cart" description="GET /api/cart" />;
}
