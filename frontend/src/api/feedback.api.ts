/**
 * Feedback API — backend base route: /api/feedback
 *
 * Documented in:
 *   - backend/README.md §3 ("Feedback — /api/feedback")
 *   - backend/postman/PanelScan-Feedback.postman_collection.json
 *
 * Only CUSTOMER can create/update/delete (own, DELIVERED orders only);
 * MODERATOR/OWNER have view-only oversight access. Not implemented yet.
 */
export {};
