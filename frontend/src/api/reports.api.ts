/**
 * Reports API — backend base route: /api/reports
 *
 * Documented in:
 *   - backend/README.md §3 ("Reports — /api/reports")
 *   - backend/postman/PanelScan-Reports.postman_collection.json
 *
 * OWNER/MODERATOR only, same role-based field redaction pattern as Analytics
 * (financial fields are OWNER-only). Not implemented yet.
 */
export {};
