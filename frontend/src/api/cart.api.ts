/**
 * Cart API — backend base route: /api/cart
 *
 * Documented in:
 *   - backend/README.md §3 ("Cart — /api/cart")
 *   - backend/postman/PanelScan-Cart.postman_collection.json
 *
 * Every route is CUSTOMER-only — always "my cart," never addressed by a cart id.
 * Not implemented yet.
 */
export {};
