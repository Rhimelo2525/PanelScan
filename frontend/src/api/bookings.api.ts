/**
 * Bookings API — backend base route: /api/bookings
 *
 * Documented in:
 *   - backend/README.md §3 ("Booking — /api/bookings")
 *   - backend/postman/PanelScan-Bookings.postman_collection.json
 *
 * GET /api/bookings/all (MODERATOR/OWNER) vs GET /api/bookings (CUSTOMER's own) —
 * two distinct endpoints, not one role-scoped route. Not implemented yet.
 */
export {};
