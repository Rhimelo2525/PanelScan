/**
 * Payments API — backend base route: /api/payments
 *
 * Documented in:
 *   - backend/README.md §3 ("Payment — /api/payments")
 *   - backend/postman/PanelScan-Payments.postman_collection.json
 *
 * POST /create redirects the customer to a PayMongo-hosted checkoutUrl — this
 * app does not build its own payment form. Not implemented yet.
 */
export {};
