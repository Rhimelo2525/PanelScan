/**
 * Analytics API — backend base route: /api/analytics
 *
 * Documented in:
 *   - backend/README.md §3 ("Analytics Dashboard — /api/analytics")
 *   - backend/postman/PanelScan-Analytics.postman_collection.json
 *
 * OWNER/MODERATOR only. Several endpoints return different fields depending on
 * role (OWNER gets financial figures MODERATOR does not) — same endpoint, two
 * response shapes. Not implemented yet.
 */
export {};
