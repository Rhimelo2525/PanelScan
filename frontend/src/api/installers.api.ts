/**
 * Installers API — backend base route: /api/installers
 *
 * Documented in:
 *   - backend/README.md §3 ("Installer — /api/installers")
 *   - backend/postman/PanelScan-Installers.postman_collection.json
 *
 * Every route requires MODERATOR specifically — OWNER has NO access at all to
 * this module (not even read), so no OWNER-facing screen should call this file.
 * Not implemented yet.
 */
export {};
