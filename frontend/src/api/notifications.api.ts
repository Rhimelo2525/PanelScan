/**
 * Notifications API — backend base route: /api/notifications
 *
 * Documented in:
 *   - backend/README.md §3 ("Notifications — /api/notifications")
 *   - backend/postman/PanelScan-Notifications.postman_collection.json
 *
 * Every role only ever sees their own notifications — there is no "view all"
 * mode. Not implemented yet.
 */
export {};
