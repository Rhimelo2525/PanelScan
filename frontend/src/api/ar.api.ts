/**
 * AR API — backend base route: /api/ar
 *
 * Documented in:
 *   - backend/README.md §3 ("AR Support — /api/ar")
 *   - backend/postman/PanelScan-AR.postman_collection.json
 *
 * The backend only persists measurement data and calculates a panel estimate
 * from it — the actual AR camera/scanning experience is entirely a mobile-app
 * concern with no backend code involved. Not implemented yet.
 */
export {};
