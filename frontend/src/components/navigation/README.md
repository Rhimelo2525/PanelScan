# components/navigation

Small reusable navigation-adjacent UI (tab bar icons, header buttons, back buttons)
consumed by the navigator files in `src/navigation/`. The navigators themselves — the
actual route/screen wiring — live in `src/navigation/`, not here.
