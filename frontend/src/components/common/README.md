# components/common

Generic, feature-agnostic building blocks used across the app: buttons, text inputs,
loading spinners, empty states, error banners, screen containers. Nothing here should
import from `features/` or `api/` — this layer knows nothing about PanelScan's domain.
