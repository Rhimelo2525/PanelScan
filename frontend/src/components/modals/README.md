# components/modals

Reusable modal/dialog primitives (confirmation dialogs, bottom sheets). Domain-specific
modal content (e.g. "confirm cancel order") still lives in the owning `features/<name>/`
folder.
