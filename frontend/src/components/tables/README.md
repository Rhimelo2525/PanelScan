# components/tables

Reusable table/list primitives for the MODERATOR and OWNER web-admin-style screens
(paginated list headers, sortable column headers, pagination controls) — matches the
`page`/`limit`/`total`/`totalPages` shape every paginated backend list endpoint
returns (`types/api.ts`'s `PaginationMeta`).
