# components/feedback

UI feedback primitives: toasts/snackbars, inline validation messages, loading and
error states shown in response to API calls. (Not to be confused with the `Feedback`
*backend module* — customer order ratings/reviews — which lives in `features/feedback/`.)
