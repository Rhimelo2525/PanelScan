# components/cards

Reusable card-style list-item presentational components (e.g. a generic summary card
shape reused across product lists, order lists, booking lists). Domain-specific card
content still lives in the owning `features/<name>/` folder.
