# features/analytics

OWNER/MODERATOR analytics dashboard UI, built on `api/analytics.api.ts`. Several
endpoints return different fields depending on role (OWNER gets financial
figures MODERATOR does not) from the same endpoint.
