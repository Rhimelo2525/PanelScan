# features/installers

MODERATOR-only installer roster management UI, built on `api/installers.api.ts`.
OWNER has zero access to this module (not even read) — do not build an
OWNER-facing screen against this feature.
